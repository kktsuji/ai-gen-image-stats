# Pinned for reproducibility. See requirements.txt for the pin basis.
# pytest*/ipython/jupyter/notebook/sphinx* match the proven GPU image;
# ruff/pyright (lint, CPU-only) are pinned to the local venv.

# Include all core dependencies
-r requirements.txt

# Testing Framework
pytest==9.0.2
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-xdist==3.8.0  # Parallel test execution

# Code Quality
ruff==0.15.5
pyright==1.1.408

# Development Tools
ipython==9.10.0
jupyter==1.1.1
notebook==7.5.3

# Documentation (optional)
sphinx==9.1.0
sphinx-rtd-theme==3.1.0
