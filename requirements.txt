# Pinned for reproducibility.
# Versions match the proven GPU image
# kktsuji/nvidia-cuda12.8.1-cudnn-runtime-ubuntu24.04 (built 2026-02-18),
# except tabulate (CPU-only, runs in venv) which is pinned to the local env.
# Python 3.13.12 (see .python-version). Do not bump during an experiment campaign.
#
# Known advisories accepted to keep this file consistent with the proven image
# (local pipeline, trusted inputs, not network-exposed):
#   - Pillow 12.1.1: CVEs fixed in 12.2.0 (image parsing). Not bumped — that
#     would desync requirements from the runtime (the image ships 12.1.1).
#   - torch 2.10.0: PYSEC-2026-139 (last-affected); no patched release exists yet.
# Revisit both only via a deliberate, re-validated image rebuild.

# Core Deep Learning
torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0

# Scientific Computing
numpy==2.3.5
scipy==1.17.0

# Image Processing
Pillow==12.1.1

# Data Science & Visualization
matplotlib==3.10.8
pandas==3.0.0
seaborn==0.13.2
scikit-learn==1.8.0
tabulate==0.10.0

# Metrics & Evaluation
numba==0.63.1
umap-learn==0.5.11

# Configuration & CLI
pyyaml==6.0.3

# Logging & Visualization
tensorboard==2.20.0

# Utilities
requests==2.32.5
python-dotenv==1.2.1
tqdm==4.67.3
